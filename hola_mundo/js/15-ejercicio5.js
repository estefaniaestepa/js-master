'use strict'

/*MUESTRE TODOS LOS NUMEROS DIVISORES DE UN NUMERO INTRODUCIDO EN UN PROMPT*/

var numero1 =parseInt(prompt("Mete un numero",1)); /*como en los anteriores meteremos un parseint para convertir a numeo y un prompt*/

for(var i = 1; i <=numero1; i++){ /*el bucle que utilizaremos es un for, porque vamos a ir desde el numero 1 hasta el numero introducido por el teclado*/
    /*el for esta formado por tres partes la primera es el contador o el inicializador en este caso es 1, la segunda parte seria la comparacion o condicion(mientras que i sea igual o menor al nuye)
console.log("Divisor:")
}